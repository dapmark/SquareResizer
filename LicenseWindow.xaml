<Window x:Class="ImageSquareResizer.LicenseWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
        Title="License and third-party components"
        Width="760"
        Height="640"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Icon="Assets/app.ico"
        PreviewKeyDown="Window_OnPreviewKeyDown">
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#202020" />
        <SolidColorBrush x:Key="InputBackgroundBrush" Color="#252526" />
        <SolidColorBrush x:Key="MainTextBrush" Color="#D4D4D4" />
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#AAAAAA" />
        <SolidColorBrush x:Key="WindowBorderBrush" Color="#292929" />
        <SolidColorBrush x:Key="ButtonBorderBrush" Color="#303030" />
        <SolidColorBrush x:Key="AccentBrush" Color="#007ACC" />
        <SolidColorBrush x:Key="TitleButtonForegroundBrush" Color="#D4D4D4" />
        <SolidColorBrush x:Key="SoftButtonHoverBackgroundBrush" Color="#333333" />
        <SolidColorBrush x:Key="SoftButtonPressedBackgroundBrush" Color="#3E3E42" />
        <SolidColorBrush x:Key="ScrollThumbBrush" Color="#686868" />
        <SolidColorBrush x:Key="ScrollThumbHoverBrush" Color="#858585" />
        <SolidColorBrush x:Key="ScrollThumbPressedBrush" Color="#A0A0A0" />

        <Style x:Key="LicenseScrollThumbStyle" TargetType="{x:Type primitives:Thumb}">
            <Setter Property="MinHeight" Value="6" />
            <Setter Property="Background" Value="{DynamicResource ScrollThumbBrush}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="ClipToBounds" Value="False" />
            <Setter Property="UseLayoutRounding" Value="True" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type primitives:Thumb}">
                        <Grid Background="Transparent"
                              ClipToBounds="False"
                              UseLayoutRounding="True"
                              SnapsToDevicePixels="True">
                            <Border x:Name="ThumbCapsule"
                                    Width="5"
                                    MinHeight="6"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Stretch"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="2.5"
                                    UseLayoutRounding="True"
                                    SnapsToDevicePixels="True">
                                <Border.RenderTransform>
                                    <TranslateTransform X="2.5" />
                                </Border.RenderTransform>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbCapsule" Property="Background" Value="{DynamicResource ScrollThumbHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbCapsule" Property="Background" Value="{DynamicResource ScrollThumbPressedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LicenseScrollTrackButtonStyle" TargetType="{x:Type RepeatButton}">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border Background="Transparent" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="LicenseVerticalScrollBarTemplate" TargetType="{x:Type ScrollBar}">
            <Grid Background="Transparent"
                  ClipToBounds="False"
                  UseLayoutRounding="True"
                  SnapsToDevicePixels="True">
                <primitives:Track x:Name="PART_Track"
                                  Focusable="False"
                                  IsDirectionReversed="True">
                    <primitives:Track.DecreaseRepeatButton>
                        <RepeatButton Command="ScrollBar.PageUpCommand"
                                      Style="{StaticResource LicenseScrollTrackButtonStyle}" />
                    </primitives:Track.DecreaseRepeatButton>
                    <primitives:Track.Thumb>
                        <primitives:Thumb MinHeight="6"
                                          Style="{StaticResource LicenseScrollThumbStyle}" />
                    </primitives:Track.Thumb>
                    <primitives:Track.IncreaseRepeatButton>
                        <RepeatButton Command="ScrollBar.PageDownCommand"
                                      Style="{StaticResource LicenseScrollTrackButtonStyle}" />
                    </primitives:Track.IncreaseRepeatButton>
                </primitives:Track>
            </Grid>
        </ControlTemplate>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Width" Value="11" />
            <Setter Property="Height" Value="Auto" />
            <Setter Property="Margin" Value="0,2,0,2" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="ClipToBounds" Value="False" />
            <Setter Property="UseLayoutRounding" Value="True" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template" Value="{StaticResource LicenseVerticalScrollBarTemplate}" />
        </Style>

        <Style x:Key="LicenseTextBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="{DynamicResource MainTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="SelectionBrush" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Padding" Value="14,12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"
                                          HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid x:Name="RootGrid" Margin="14">
        <Border Background="{DynamicResource WindowBackgroundBrush}"
                CornerRadius="18"
                SnapsToDevicePixels="True">
            <Border.Effect>
                <DropShadowEffect BlurRadius="24"
                                  ShadowDepth="4"
                                  Opacity="0.22"
                                  Color="#000000" />
            </Border.Effect>
        </Border>

        <Border BorderBrush="{DynamicResource WindowBorderBrush}"
                BorderThickness="1"
                CornerRadius="18"
                Background="{DynamicResource WindowBackgroundBrush}"
                Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="12" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0"
                            Margin="0,0,42,0"
                            Background="Transparent"
                            MouseLeftButtonDown="Header_OnMouseLeftButtonDown">
                    <TextBlock x:Name="TitleTextBlock"
                               FontFamily="Segoe UI Variable Display"
                               FontSize="21"
                               FontWeight="Bold"
                               Foreground="{DynamicResource MainTextBrush}" />
                </StackPanel>

                <TextBox x:Name="LicenseTextBox"
                         Grid.Row="2"
                         Style="{StaticResource LicenseTextBoxStyle}"
                         IsReadOnly="True"
                         IsReadOnlyCaretVisible="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         ClipToBounds="False"
                         UseLayoutRounding="True"
                         SnapsToDevicePixels="True"
                         FontFamily="Segoe UI"
                         FontSize="13" />
            </Grid>
        </Border>

        <Button x:Name="CloseButton"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,10,10,0"
                Style="{StaticResource AuxiliaryWindowCloseButtonStyle}"
                Click="CloseButton_OnClick" />
    </Grid>
</Window>
